import React from 'react';
import {render, screen} from '@testing-library/react';

describe('Test for countryEvaluator function', () => {
  test('should...', () => {
    expect(2 + 2).toBe(4);
  });
});
