import React from 'react';
import {render, screen} from '@testing-library/react';

describe('<ReactUIBucketProvider/>', () => {
  test('should render <ReactUIBucketProvider/>', () => {
    expect(2 + 2).toBe(4);
  });
});
