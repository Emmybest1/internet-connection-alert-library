import React from 'react';
import {render, screen} from '@testing-library/react';

describe('<ApiSuccessEngine/>', () => {
  test('should render <ApiSuccessEngine/>', () => {
    expect(2 + 2).toBe(4);
  });
});
