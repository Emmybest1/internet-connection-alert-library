import React from 'react';
import {render, screen} from '@testing-library/react';

describe('<FallBackEngine/>', () => {
  test('should render <FallBackEngine/>', () => {
    expect(2 + 2).toBe(4);
  });
});
